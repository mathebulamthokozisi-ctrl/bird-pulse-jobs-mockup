# Visual Audit — Editor Issues Found

## Estimate Editor
- GOOD: Scope of Work card renders, sections visible (Materials, Equipment, Labour, Preliminaries)
- GOOD: Section totals live-calculating (Materials R 19,954, Labour R 6,700, Prelim R 600)
- GOOD: Sidebar shows Summary, Margin vs Quote, By Category bar chart, Document Info
- ISSUES:
  1. Unit dropdown truncated — only shows "Each, Metre, m², m³, Hour" — missing "Day" and "Lot"
  2. Equipment section shows header "TYPE" instead of "CATEGORY" — and the empty state row is missing (shows just the header)
  3. Labour section shows "TYPE" header — should be "Type" (internal/tradesmen/subcontractor)
  4. Materials section only shows 2 items — the rest (labour/prelim items) are in correct sections but the scroll is needed
  5. The "Add Line" topbar button adds to materials by default with no section picker
  6. Stage strip (DRAFT/IN REVIEW/APPROVED/FINALISED) is cramped and overlapping with topbar buttons
  7. Internal Notes card at bottom is just a bare icon — no textarea
  8. No "Add Method Statement" or "Add Assumptions" fields visible (were added in HTML but may not be rendering)
  9. Grand total footer card missing VAT breakdown — only shows "Subtotal (excl. VAT)"

## Quote Editor — need to open
## Invoice Editor — need to open  
## Actual Costs Editor — need to open
## Jobcard Editor — need to open
